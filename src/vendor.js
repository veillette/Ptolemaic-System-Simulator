// Bootstrap 5 no longer depends on jQuery. The bundle build includes Popper,
// and importing it wires up the data-bs-* API used by the modals in index.html.
import 'bootstrap/dist/js/bootstrap.bundle.min.js';
